<Window x:Class="StockNewsNotifier.Views.AddWatchDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Watch"
        Height="220"
        Width="320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Exchange" Margin="0,0,0,4"/>
            <TextBox x:Name="ExchangeTextBox" Height="28" />

            <TextBlock Text="Ticker" Margin="0,12,0,4"/>
            <TextBox x:Name="TickerTextBox" Height="28" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Width="80" Margin="0,0,8,0" Content="Cancel" IsCancel="True"/>
            <Button Width="80" Content="Add" IsDefault="True" Click="OnAddClicked"/>
        </StackPanel>
    </Grid>
</Window>
